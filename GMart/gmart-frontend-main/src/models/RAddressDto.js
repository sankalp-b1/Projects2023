export default class RAddressDto {
	constructor(shopNo, streetName, locality, city, state, pincode,id) {
		this.shopNo = shopNo;
		this.streetName = streetName;
		this.locality = locality;
		this.city = city;
		this.state = state;
		this.pincode = pincode;
		this.id = id;
	}
}
